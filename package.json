{
  "name": "kitty",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "bun --filter @kitty/web dev",
    "build": "bun --filter @kitty/web build",
    "preview": "bun --filter @kitty/web preview",
    "typecheck": "bun --filter @kitty/web typecheck",
    "lint": "bun --filter @kitty/web lint"
  }
}
